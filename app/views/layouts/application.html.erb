<!DOCTYPE html>
<html>
  <head>
    <title>IClone</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
    <header>
        <ul class="top_menu row">
          <% if logged_in? %>
            <li class="col-md-2"><%= "  ImageShare" %></li>
            <li class="col-md-2"><%= link_to "メイン画面" , pictures_path%></li>
            <li class="col-md-2"><%= link_to "ユーザー一覧" , users_path %></li>
            <li class="col-md-2"><%= link_to "お気に入り一覧", favorites_path(current_user.id) %></li>
            <li class="col-md-2"><%= link_to "プロフィール編集", edit_user_path(current_user.id) %></li>
            <li class="col-md-2"><%= link_to "ログアウト", session_path(current_user.id), method: :delete %></li>
          <% else %>
            <li class="col-md-2"><%= "  ImageShare" %></li>
            <li class="col-md-8"></li>
            <li class="col-md-2"><%= link_to "Home" %></li>
           <% end %>
        </ul>
    </header>
    <body>
      <% flash.each do |key, value| %>
        <%= content_tag(:div, value, class: "#{key}") %>
      <% end %>

      <%= yield %>
    </body>
</html>
