<h3>詳細画面</h3>
  投稿<br>
  <%= @picture.content %><br>
  写真<br>
  <%= image_tag(@picture.image.url) if @picture.image && @picture.image.url %><br>
  <button><%= link_to "戻る", pictures_path %></button>